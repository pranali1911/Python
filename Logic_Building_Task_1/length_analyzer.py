messages = ["Hi", "Welcome to the platform", "OK"]

# Calculate the length of each message
for message in messages:
    length = len(message)
    print(f"The length of the message '{message}' is: {length}")